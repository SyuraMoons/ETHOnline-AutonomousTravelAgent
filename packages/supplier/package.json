{
  "name": "@sh/supplier",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:prod": "tsx src/index.ts",
    "check-types": "tsc --noEmit --incremental && tsc -p tsconfig.scripts.json",
    "flights:generate": "tsx scripts/scrape-flights.ts",
    "test": "node --import tsx --test src/**/*.test.ts src/**/**/*.test.ts",
    "stays:generate": "tsx scripts/generate-stays.ts",
    "activities:generate": "tsx scripts/generate-activities.ts",
    "data:generate": "npm run flights:generate && npm run stays:generate && npm run activities:generate",
    "gen-signing-key": "tsx scripts/gen-signing-key.ts",
    "data:inspect": "tsx scripts/inspect-data.ts",
    "sign-identity-claim": "tsx scripts/sign-identity-claim.ts",
    "preflight": "tsx scripts/preflight.ts",
    "setup-env": "tsx scripts/setup-env.ts"
  },
  "//tsx": "A RUNTIME dependency, not a dev one: production runs `tsx src/index.ts` (see start:prod and the Dockerfile). @sh/contracts' main is raw TypeScript, so `node dist/index.js` cannot load it.",
  "//@x402/hedera": "PINNED, exact, deliberately NOT 2.13.2: this package only builds seller-side payment requirements (ExactHederaScheme from '@x402/hedera/exact/server') and never verifies a payer signature, so it is unaffected by the 2.25+ verifyPayerSignature landmine that forces the facilitator and frontend to stay on 2.13.2 (see facilitator/package.json and AGENTS.md). Pinned to 2.25.0 instead so this package resolves its own @x402/core@2.25.0 (the newer FacilitatorConfig/beforeHandlerSettlement API src/services/x402/server.ts uses) rather than sharing the frontend workspace's 2.14.0.",
  "dependencies": {
    "@sh/contracts": "*",
    "@x402/core": "^2.14.0",
    "@x402/hedera": "2.25.0",
    "dotenv": "^16.6.1",
    "express": "^4.21.1",
    "tsx": "^4.19.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.16.11",
    "typescript": "^5.8.2"
  },
  "packageManager": "npm@10.0.0"
}
