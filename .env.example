# Local infrastructure config for `docker-compose.yml` (npm run infra:up).
# Copy to `.env` and fill in the facilitator credentials.

# --- Self-hosted x402 Hedera facilitator ---
FACILITATOR_PORT=4020
X402_NETWORK=hedera:testnet
#
# Fee-payer credentials (ECDSA). Required because Hedera x402 settlement is a native
# TransferTransaction: the buyer's wallet only partially signs (authorizing the HBAR move);
# the facilitator must co-sign as fee payer, pay network fees, and submit to consensus.
# The Next.js app never sees this key — only the facilitator container does.
# Create a dedicated ECDSA testnet account, fund it with HBAR, and paste id + key here.
FACILITATOR_ACCOUNT_ID=0.0.xxxxxx
FACILITATOR_PRIVATE_KEY=
#
# Which account is advertised to buyers as `extra.feePayer` — meaning "the account that owns
# the transaction id", which Hedera requires to be the SPENDER on an approved (allowance)
# transfer. Two mutually exclusive modes, one instance can only do one:
#
#   ALLOWANCE MODE — set to the planner's AGENT_ACCOUNT_ID. The agent spends the user's HBAR
#     under a HIP-336 allowance and pays the node fee; the facilitator verifies and submits.
#   TREASURY MODE  — leave blank. The facilitator sponsors fees, the agent pays from its own
#     balance (the original behaviour).
FACILITATOR_ADVERTISED_FEE_PAYER=
# Optional custom consensus node endpoint for private Hedera networks.
HEDERA_NODE_URL=
